(() => {
  "use strict";
  const APP_VERSION="0.13.12",CITY_ENDPOINT="https://geocoding-api.open-meteo.com/v1/search",DESTINATION_ENDPOINT="https://photon.komoot.io/api",CITY_PASS_RADIUS_KM=3;
  const NEARBY_CODES=new Set(["AR","UY","PY","BO","PE","CL","CO","VE","GY","SR","GF","PA","CR","NI","HN","SV","GT","BZ","EC"]);
  const DESTINATION_KEYS=new Set(["tourism","amenity","leisure","natural","historic","man_made","aeroway","railway","shop","office","craft","sport","place"]),ADMINISTRATIVE_PLACE_VALUES=new Set(["city","town","village","municipality","state","country","county","suburb","neighbourhood","borough"]);
  const destinationHint=/\b(camping|camp|cachoeira|waterfall|parque|park|pousada|hotel|hostel|museu|museum|fazenda|mirante|viewpoint|praia|beach|reserva|sitio|gruta|caverna|cave|estacao|station|aeroporto|airport|restaurante|restaurant|termas?|cathedral|catedral|igreja|church|temple|templo|biblioteca|library)\b/i;
  if(typeof window.fetch!=="function"||typeof placeFromCityResult!=="function"||typeof cityConquestsForTrip!=="function")return;
  const previousFetch=window.fetch.bind(window),nativeFetch=window.__mvNativeFetch||previousFetch,basePlaceFromCityResult=placeFromCityResult,baseCityConquestsForTrip=cityConquestsForTrip,destinationCache=new Map(),municipalityCache=new Map();
  const stats=window.MinhasViagensPlaceSearchStats={version:APP_VERSION,searches:0,destinationResults:0,failures:0,lastError:""};
  function normalize(v){return String(v||"").normalize("NFD").replace(/[\u0300-\u036f]/g,"").toLowerCase().replace(/[^a-z0-9]+/g," ").trim();}
  function categoryLabel(p){const value=String(p?.osm_value||"").toLowerCase(),key=String(p?.osm_key||"").toLowerCase(),labels={camp_site:"Camping",caravan_site:"Camping",hotel:"Hotel",hostel:"Hostel",guest_house:"Pousada",chalet:"Hospedagem",museum:"Museu",attraction:"Atração",viewpoint:"Mirante",waterfall:"Cachoeira",cave_entrance:"Caverna",nature_reserve:"Reserva natural",park:"Parque",beach:"Praia",restaurant:"Restaurante",aerodrome:"Aeroporto",station:"Estação",cathedral:"Catedral",church:"Igreja",library:"Biblioteca"};return labels[value]||({tourism:"Destino",natural:"Local natural",historic:"Local histórico"}[key]||"Local");}
  function isDestinationFeature(feature){const p=feature?.properties||{},c=feature?.geometry?.coordinates,key=String(p.osm_key||"").toLowerCase(),value=String(p.osm_value||"").toLowerCase();if(!p.name||!Array.isArray(c)||c.length<2||!Number.isFinite(Number(c[0]))||!Number.isFinite(Number(c[1])))return false;if(!DESTINATION_KEYS.has(key))return false;if(key==="place"&&ADMINISTRATIVE_PLACE_VALUES.has(value))return false;return true;}
  function municipalityIdentity(name,state,countryCode){return`${normalize(name)}|${normalize(state)}|${String(countryCode||"").toUpperCase()}`;}
  async function resolveMunicipality(p,signal){const city=String(p?.city||"").trim();if(!city)return null;const state=String(p?.state||"").trim(),countryCode=String(p?.countrycode||"").toUpperCase(),key=municipalityIdentity(city,state,countryCode);if(municipalityCache.has(key))return municipalityCache.get(key);const response=await previousFetch(`${CITY_ENDPOINT}?name=${encodeURIComponent(city)}&count=20&language=pt&format=json`,{signal,cache:"force-cache"});if(!response.ok)return null;const data=await response.json(),candidates=(Array.isArray(data?.results)?data.results:[]).filter(i=>Number.isFinite(Number(i.latitude))&&Number.isFinite(Number(i.longitude))&&normalize(i.name)===normalize(city)&&(!countryCode||String(i.country_code||"").toUpperCase()===countryCode)&&(!i.feature_code||String(i.feature_code).toUpperCase().startsWith("PPL"))),selected=candidates.find(i=>!state||normalize(i.admin1)===normalize(state))||candidates[0];if(!selected)return null;const municipality={label:[selected.name,selected.admin1,selected.country].filter(Boolean).join(", "),city:selected.name,region:selected.admin1||state,country:selected.country||p.country||"",countryCode:selected.country_code||countryCode,lat:Number(selected.latitude),lng:Number(selected.longitude),geonamesId:selected.id||null};municipalityCache.set(key,municipality);return municipality;}
  async function destinationResult(feature,signal){const p=feature.properties||{},c=feature.geometry.coordinates;let municipality=null;try{municipality=await resolveMunicipality(p,signal);}catch(error){if(error?.name==="AbortError")throw error;}const city=municipality?.city||p.city||p.county||"",state=municipality?.region||p.state||"",country=municipality?.country||p.country||"",category=categoryLabel(p);return{id:`osm-${p.osm_type||"X"}-${p.osm_id||`${c[1]}-${c[0]}`}`,name:p.name,latitude:Number(c[1]),longitude:Number(c[0]),admin1:[category,city,state].filter(Boolean).join(" · "),country,country_code:municipality?.countryCode||p.countrycode||"",_mvPlaceType:"destination",_mvDestinationCategory:category,_mvState:state,_mvMunicipality:municipality,_mvMunicipalityName:city,_mvOsmType:p.osm_type||"",_mvOsmId:p.osm_id||null};}
  function directDestinationNameMatch(name,query){const n=normalize(name),q=normalize(query);if(!n||!q||q.length<3)return false;return n===q||n.startsWith(q)||(q.length>=6&&n.split(" ").slice(0,Math.max(1,q.split(" ").length)).join(" ").startsWith(q));}
  async function searchDestinations(query,signal){const cacheKey=normalize(query);if(destinationCache.has(cacheKey))return destinationCache.get(cacheKey);const url=new URL(DESTINATION_ENDPOINT);url.searchParams.set("q",query);url.searchParams.set("limit","20");const response=await nativeFetch(url.href,{signal,cache:"default",mode:"cors"});if(!response.ok)throw new Error(`Busca de locais ${response.status}`);const data=await response.json(),allowByCategory=destinationHint.test(normalize(query));const features=(Array.isArray(data?.features)?data.features:[]).map((feature,index)=>({feature,index})).filter(item=>isDestinationFeature(item.feature)).filter(item=>allowByCategory||directDestinationNameMatch(item.feature.properties?.name,query)).sort((a,b)=>{const exactA=normalize(a.feature.properties?.name)===normalize(query)?0:1,exactB=normalize(b.feature.properties?.name)===normalize(query)?0:1;if(exactA!==exactB)return exactA-exactB;const codeA=String(a.feature.properties?.countrycode||"").toUpperCase(),codeB=String(b.feature.properties?.countrycode||"").toUpperCase(),group=code=>code==="BR"?0:NEARBY_CODES.has(code)?1:2;return group(codeA)-group(codeB)||a.index-b.index;}).slice(0,8).map(item=>item.feature);const results=await Promise.all(features.map(feature=>destinationResult(feature,signal)));destinationCache.set(cacheKey,results);if(destinationCache.size>80)destinationCache.delete(destinationCache.keys().next().value);stats.searches++;stats.destinationResults+=results.length;stats.lastError="";return results;}
  function mergeResults(cities,destinations,query){const q=normalize(query),output=[],seen=new Set(),cleanCities=(cities||[]).filter(item=>!item.feature_code||String(item.feature_code).toUpperCase().startsWith("PPL")),exactCity=cleanCities.some(item=>normalize(item.name)===q),matchedDestinations=(destinations||[]).filter(item=>destinationHint.test(q)||directDestinationNameMatch(item.name,query)),exactDestination=matchedDestinations.some(item=>normalize(item.name)===q),append=items=>{for(const item of items||[]){if(output.length>=12)break;const key=item?._mvPlaceType==="destination"?String(item.id):`${normalize(item?.name)}|${normalize(item?.admin1)}|${String(item?.country_code||"").toUpperCase()}`;if(!key||seen.has(key))continue;seen.add(key);output.push(item);}};if(exactCity){append(cleanCities);append(matchedDestinations);}else if(destinationHint.test(q)||exactDestination){append(matchedDestinations);append(cleanCities);}else{append(cleanCities);append(matchedDestinations);}return output;}
  window.fetch=async function placeSearchFetch(input,init={}){const requestUrl=typeof input==="string"?input:input?.url;if(!requestUrl||!requestUrl.startsWith(CITY_ENDPOINT))return previousFetch(input,init);const parsed=new URL(requestUrl,location.href),query=parsed.searchParams.get("name")?.trim()||"";if(query.length<3)return previousFetch(input,init);const cityPromise=previousFetch(input,init).then(async response=>{if(!response.ok)throw new Error(`Busca de cidades ${response.status}`);const data=await response.json();return Array.isArray(data?.results)?data.results:[];}),destinationPromise=searchDestinations(query,init?.signal||null).catch(error=>{if(error?.name==="AbortError")throw error;stats.failures++;stats.lastError=String(error?.message||error||"Busca de locais indisponível");return[];});const[cityOutcome,destinationOutcome]=await Promise.allSettled([cityPromise,destinationPromise]);if(init?.signal?.aborted)throw new DOMException("Cancelado","AbortError");if(cityOutcome.status==="rejected"&&destinationOutcome.status==="rejected")throw cityOutcome.reason;const cities=cityOutcome.status==="fulfilled"?cityOutcome.value:[],destinations=destinationOutcome.status==="fulfilled"?destinationOutcome.value:[];return new Response(JSON.stringify({results:mergeResults(cities,destinations,query)}),{status:200,headers:{"Content-Type":"application/json; charset=utf-8"}});};
  placeFromCityResult=function placeFromCityOrDestination(result){if(result?._mvPlaceType!=="destination")return basePlaceFromCityResult(result);const municipality=result._mvMunicipality?{...result._mvMunicipality}:null,state=municipality?.region||result._mvState||"",country=municipality?.country||result.country||"",city=municipality?.city||result._mvMunicipalityName||"";return{label:[result.name,city,state,country].filter((value,index,values)=>value&&values.indexOf(value)===index).join(", "),name:result.name,city:result.name,region:state,country,countryCode:municipality?.countryCode||result.country_code||"",lat:Number(result.latitude),lng:Number(result.longitude),geonamesId:result.id||null,placeType:"destination",destinationCategory:result._mvDestinationCategory||"Local",municipality,osmType:result._mvOsmType||"",osmId:result._mvOsmId||null};};
  function routePassesMunicipality(trip,municipality,radiusKm=CITY_PASS_RADIUS_KM){const target=[Number(municipality?.lat),Number(municipality?.lng)];if(!target.every(Number.isFinite))return false;const route=typeof tripLatLngs==="function"?tripLatLngs(trip):[];if(!Array.isArray(route)||route.length<2)return false;for(let i=1;i<route.length;i++){const a=route[i-1],b=route[i];if(!Array.isArray(a)||!Array.isArray(b))continue;if(typeof pointSegmentDistanceKm==="function"&&pointSegmentDistanceKm(target,a,b)<=radiusKm)return true;}return false;}
  function isDestinationPlace(place){return place?.placeType==="destination";}
  cityConquestsForTrip=function cityConquestsIncludingPassedDestinationCities(trip){const regularTrip={...trip,startPlace:isDestinationPlace(trip?.startPlace)?null:trip?.startPlace,stopPlaces:(trip?.stopPlaces||[]).filter(place=>!isDestinationPlace(place)),endPlace:isDestinationPlace(trip?.endPlace)?null:trip?.endPlace},cities=baseCityConquestsForTrip(regularTrip),seen=new Set(cities.map(city=>municipalityIdentity(city.city,city.region,city.countryCode))),destinations=[trip?.startPlace,...(trip?.stopPlaces||[]),trip?.endPlace].filter(isDestinationPlace);for(const destination of destinations){const municipality=destination.municipality;if(!municipality||!routePassesMunicipality(trip,municipality))continue;const key=municipalityIdentity(municipality.city,municipality.region,municipality.countryCode);if(!key||seen.has(key))continue;seen.add(key);cities.push({label:municipality.label||[municipality.city,municipality.region,municipality.country].filter(Boolean).join(", "),city:municipality.city,region:municipality.region||"",country:municipality.country||"",countryCode:municipality.countryCode||"",lat:Number.isFinite(Number(municipality.lat))?Number(municipality.lat):undefined,lng:Number.isFinite(Number(municipality.lng))?Number(municipality.lng):undefined});}return cities;};
  window.MinhasViagensPlaceSearch={version:APP_VERSION,cityPassRadiusKm:CITY_PASS_RADIUS_KM,stats,searchDestinations,isDestinationFeature,isDestinationPlace,routePassesMunicipality,directDestinationNameMatch,mergeResults};
  const brandCopy=document.querySelector(".brand p");if(brandCopy)brandCopy.textContent=brandCopy.textContent.replace(/v\d+\.\d+\.\d+/,`v${APP_VERSION}`);console.info(`Minhas Viagens ${APP_VERSION}: cidades têm prioridade; destinos específicos só aparecem por nome/categoria explícita.`);
})();